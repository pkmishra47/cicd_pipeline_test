package org.apache.nifi.processors.daxoperation.models.dax;

public enum TestData {
    special_result,
    uhid,
    order_id,
    record_group_id,
    service_code,
    hcu_id,
    created_at,
    received_on,
    delay,
    doctor_id,
    hcu_name,
    doctor_name,
    service_name,
    department_name,
    static_comment,
    patienttype,
    identifier,
    visitId,
    specimen,
    checked_by,
    signing_doc_name,
    doc_sign,
    refer_doc_name,
    collected_on,
    first_report_printed_on,
    printed_on,
    drn,
    impression,
    interpretation,
    special_result_values,
    parameter_id,
    parametername_new,
    parameter_name,
    reference_val,
    unit,
    result,
    sequence,
    updated_at,
    max,
    min,
    condition,
    result_date,
    bill_id
}
